name: Publish to npm
'on':
  release:
    types:
    - published
  workflow_dispatch: {}
jobs:
  publish:
    uses: dzackgarza/opencode-plugins/.github/workflows/bun-plugin-publish.yml@main
    with:
      package_name: '@dzackgarza/opencode-zotero-plugin'
    secrets: inherit
